---
sidebar_position: 3
---

# `PTxInInfo`

:::info
Modified from V2 version. 
:::

It represents a transaction input.

definition:

```ts
export const PTxInInfo = pstruct({
    PTxInInfo: {
        utxoRef: PTxOutRef.type,
        resolved: PTxOut.type
    }
});
```

### `utxoRef`

type: [`PTxOutRef.type`](./PTxOutRef)


### `resolved`

type: [`PTxOut.type`](../../V2/Tx/PTxOut)

