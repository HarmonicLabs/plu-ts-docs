---
sidebar_position: 21
---

# `PValue`

It represents a ledger Value.

Definition:

```ts
export const PAssetsEntry = palias(
    pair(
        PTokenName.type,
        int
    )
)

export const PValueEntry = palias(
    pair(
        PCurrencySymbol.type,
        list( PAssetsEntry.type )
    )
)

export const PValue = palias(
    list( PValueEntry.type )
)
```

The value implements all the properties defined on the [`TermList`](../../stdlib/TermList) utility term.


See also:

- [`PCurrencySymbol`](./PCurrencySymbol)
- [`PTokenName`](./PTokenName)
- [`TermInt`](../../stdlib/TermInt)